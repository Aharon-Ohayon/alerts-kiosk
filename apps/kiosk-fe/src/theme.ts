import { ThemeConfig, theme } from 'antd';

export const alertTheme: ThemeConfig = {
    algorithm: theme.darkAlgorithm,
    components: {
        Alert: {
            fontSizeLG: 24,
            fontSize: 16
        }
    }
};
